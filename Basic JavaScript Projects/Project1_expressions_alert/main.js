var Sent1 = "This is the beginning of the string", Sent2 = "and this is the end of the string"; //Assigning variables

var CompleteSent = Sent1 + Sent2; //Concatenating variables

document.write(CompleteSent); //Printing varibles
